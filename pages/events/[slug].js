import Layout from "@/components/Layout"

export default function EventPage() {
    // const router = useRouter()

    // console.log(router)
    return (
        //Gets the /event/events-2 route
        <Layout>
            <h1>Eventss</h1>
        </Layout>
    )
}
